{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Compose App (Cmd+R)",
      "program": "${workspaceFolder}",
      "preLaunchTask": "Launch Emulator & Install App"
    }
  ]
}
